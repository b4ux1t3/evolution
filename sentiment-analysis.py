# Determines the level of positivity for a given input.
import os
from sklearn.feature_extraction.text import CountVectorizer
from sklearn.ensemble import RandomForestClassifier
import pandas as pd
import nltk

# This function will take in an inputString and then run it through the
# trained algorithm to generate a score.
def parse(inputString)

    return 0

if __name__ == "__main__":
    pass